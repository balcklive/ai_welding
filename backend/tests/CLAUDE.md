# CLAUDE.md — backend/tests/

pytest 测试。运行 `uv run pytest`（内存 SQLite / 假客户端，绝不连远程 MySQL / MinIO）。
`tests/__init__.py` 使 backend/ 进入 sys.path，pytest 才能 `from app...` import。

## 脚本

> **⚠️ 现状（2026-09-14 核对，读下面每条前先看这里）**：本文件描述的覆盖面**与磁盘不符**。
> `9c68a83 "fix dataset browsing and remove obsolete demo tests"` 删除了 **15 个测试文件共 8023 行**：
> `test_welds.py` / `test_models.py` / `test_datasets.py` / `test_split_annotation.py` / `test_alignment.py` /
> `test_reports.py` / `test_dashboard.py` / `test_features.py` / `test_analysis.py` / `test_authz.py` /
> `test_audit_routes.py` / `test_media_prep.py` / `test_signal_ingest.py` / `test_signals_downsample.py`。
> 现盘上有 **23 个文件 / 224 用例**（`ls tests/test_*.py` 为准；2026-09-14 第二轮补齐后从 160 涨到 224），下面提到这些文件名的条目属**历史记录**。
> 被删掉的恰是"Welds CRUD / 数据集构建 / 训练 / 切分标注 / 权鉴"等核心链路的离线回归——**改动这些链路时必须补测试**
> （`test_dataset_members.py` 即为此补的）。恢复旧用例：`git checkout 9c68a83^ -- backend/tests/<file>`，
> 但其中不少断言的是已被删除的演示/兜底行为，取回后要逐条核对。
>
> 新增用例请沿用本目录既有约定：内存 SQLite + `SQLModel.metadata.create_all` + `Session(engine)`；
> 走 HTTP 的用 `StaticPool` + `dependency_overrides`；不连远程 MySQL/MinIO（存储一律 monkeypatch `app.storage.get_storage`）。

- `test_app.py`（Task 1）：`GET /api/v1/health` 统一信封 + `X-Correlation-ID` 回写。用真实 `app.main` 的 TestClient。
- `test_registration_t4b.py`（**T4b 登记改造，2026-09-14 新增**）：走真实 HTTP（`POST /registrations`）——写入只写新列（旧列恒 NULL）与旧列回读拼接、字段级 422（电流/电压/厚度/送丝/焊接速度/采样频率/来源/样本名称的边界与越界）、采集时间未来拒绝与**本地墙钟容差**、字典必选（型号/方法不在系统设置里 → 400；编辑放行原值）、**旧字段不能绕过量程**、旧客户端只发 `current_voltage` 仍可登记、`extra="forbid"`、幂等键的规范化（`180`/`180.0`/`180A/22V` 同键，不同电流不撞键），以及**迁移 `0017` 与服务层解析规则的逐形态对拍**（含 `_format_legacy` 的往返）。
- `test_dataset_members.py`（**T11 成员口径，2026-09-14 新增；R3 后扩到 8 条**）：成员来源白名单/二选一/判重，加 R3 的三条——同一份 `.csv` 通道不同结论不同、failed/running 的导入不算"已具备"、分析产物版本把 `latest_version_id` 指走后仍认得出通道。
- `test_dataset_build_e2e.py`（**T11 端到端，2026-09-14 新增，首选**）：**全程走真实 HTTP 接口 + 真实 Job 执行器**（`executor.run_job`），不碰任何私有函数——登记 `POST /registrations` → 挂载 `POST …/raw-files`（自动建数据集版本 + 自动构建任务）→ 信号导入 job → 分段 `POST …/split-tasks` job → 构建 job → 读回 `GET /datasets/{id}/versions/{vid}[/items]` 断言。3 条场景：① **锚点不进版本**（挂视频 + 建 video 标注任务生成锚点 → 成员只有 1 条基础样本、`repeat_rate == 0.0`；再挂一次文件验证基础样本**复用同一条**）；② **只收最近一次成功分段的切片**（6 切片 → 重新分段 12 切片 → 新版本只含新的 12 条、与旧的 `isdisjoint`、历史版本不受影响）；③ **failed 分段不产生成员**（窗口大于有效区间 → job failed → 构建仍只收基础样本）。隔离：内存 SQLite（StaticPool，请求 session 与 Job session 共用一个连接）+ 假 Storage（内存 dict）。**坑**：`monkeypatch` 必须返回 **SQLModel 的 `Session`**（原生 SQLAlchemy Session 没有 `.exec`）；`get_storage` 要同时打在 `app.storage` / `app.api.v1.welds` / `app.jobs.split` 三个名字上（前两处是模块级 import）；构建任务只能从库里查 `DatasetBuildTask`——`raw-files` 响应不含 job_id（T8 待补 `build_job_id`）；⑤ **删除预检与实际删除一致**（T7：有登记样本/成员引用 → 预检 blocking 且真删 409；空数据集 → 预检放行且真删成功）；⑥ **登记写 `20K`、CSV 无时间列时靠登记的采样率兜底**（`_parse_fs` 修复回归，旧正则强制要求 Hz）；⑦ **异步构建的状态出口**（T8：挂载响应带 `dataset_build`、版本列表能读 `build_status`、重试幂等且不过闸门、乱序构建不动指针）；⑧ **列表分页 / 关键字 / `?options=1` 轻量接口**（T9）；⑨ **切分单位**（T10：25 fps + 10 kHz + 83 秒 + 每切片 10 帧 → **207 个切片**、每个 CSV 4000 数据行、`window_seconds=0.4`、`rules_version=2`；无视频时按帧被 400 拒绝、按秒给出 200 采样点；窗口放不下的请求在创建阶段就被拒）。；⑩ **冻结标注快照**（T16.1：删光 `annotations` 行后**训练仍成功**——冻结生效时标签来自 `dataset_items.annotations`；没冻住会退化成单类而失败）+ **partial 不产版本**（T16.3：缺模态默认直接失败，放开后状态 partial 且不生成「特征提取」版本）+ **分段生成版本**（T16.2：`object_keys` 合并源版本文件、带产物清单）。**坑**：`signal_ingest._PARQUET_CACHE` 是**进程内** LRU，键为 `processed/{weld_id}/signals/{ingest.id}.parquet`——各用例的 weld_id 与 ingest id 会重复，**fixture 里必须清缓存**，否则读到的会是上一个用例的信号。**这条 E2E 首次运行就抓出 `POST /annotation-tasks`（signal/video）500——`analysis_annotations.py` 抽文件时漏 import `DataRecord`。**
- `test_dataset_members.py`（**T11 单元级，同批新增**）：直接测 `_samples_for_dataset_records` / `_compute_quality` / `_is_annotation_anchor` 的 5 条细粒度规则（含"无产物视为唯一"、同产物按 0.2 计重复等**在真实接口里难以构造**的判重边界）。**用户偏好：新覆盖优先写端到端**（见 `test_dataset_build_e2e.py`），本文件保留是因为它钉的判重边界在 E2E 里构造不出来。
- `test_config.py`（Task 1）：`Settings` 默认值 / `mysql_url` 拼接（不读远程）。
- `test_logging.py`（Task 1）：`_mask`/`_mask_query` 脱敏（password/token/secret → `***`）与 `_caller_from_authorization` 调用人解析。
- `test_models.py`（Task 2 + **Task 16**）：内存 SQLite 建全部 23 张表 + 索引一致性（含与迁移 `0001_initial.py` 的索引对齐防漂移）+ 关键表插入/复合唯一/环形指针；**在线 Alembic 回归**：`test_alembic_upgrade_online_real_path_executes_0003` 会在真实 MySQL 临时库按 `0002 -> 0003 -> head` 执行，现额外验证 **`data_versions.request_key`、`split_tasks.request_key/active_request_key`、`alignment_tasks.request_key/active_request_key`** 与唯一约束；**Task 16 模型中心 API**（内存 SQLite + StaticPool + 真实 app TestClient + `seed_all` + override 依赖 + executor SessionLocal 指测试引擎 + 假存储）——覆盖：列表汇总（total==3 / prod_candidates==1 / gpu_usage==42 / recent_training None）+ 详情 + 新建（同名 409 / 空名 400）；PATCH 状态流转（生产候选→实验版本→生产候选、非法状态 400、note 接受不落库）；训练端到端（POST→run_job→succeeded、result 含 metrics{mAP50,precision,recall}+loss_curve{train,val} 长度=epochs+model_version、**事务内自动生成 model_versions 实验版本 + version_no 递增 + file_key `models/{id}/weights.pt`**、假存储收到权重写、模型仓库最新版本更新）+ **readiness=暂不可训练拒绝** + **必需输入齐全/可选模态缺失仍允许** + **同 dataset_version 活动训练任务去重** + **后台 executor 自动消费 training/test/inference**；测试端到端（confusion_matrix 2×2 [[612,18],[22,596]] + metrics 数值）+ **不匹配模型/数据集版本拒绝** + **无 test split 拒绝**；推理端到端（boxes 4 元组列表 / categories / confidence / latency_ms + inference_tasks 落库）+ **损坏/伪装图片与 gif 拒绝** + **同 input_key 幂等**；404（模型/版本/跨模型版本 40402/未知任务/坏 dataset_version/base_model/空输入 400）；训练 handler 抛异常 → job failed；11 端点未登录全 401（40100）。
- `test_reports.py`（**Task 17**）：通用报告导出。同 test_datasets 基础设施（内存 SQLite + StaticPool + `seed_all` + override 依赖），**monkeypatch `app.storage.get_storage` → FakeStorage**（记录 upload_stream 字节 + presign_get，不连 MinIO）。覆盖：`type=validation, format=json` → urls + 假存储字节解析为 JSON 含 score 93.3 / 14 通过 / 1 警告 / 15 条规则、对象键 `reports/validation/{id}.json`；`format=pdf` → 字节以 `%PDF` 开头；`type=data-list, ref_ids=[]` → 全量单份（ref_id=`all`、total==4）+ 按 weld_id 过滤单条 + 未知标识 404；`type=analysis`（真实版本 id）→ 通用模板 200 + sections；**Task 4 修复回归**：`analysis/features/test` 三类 PDF 都能导出 `%PDF` 且 JSON 仍准确引用当前实体；**data-list ownership**：非管理员空 `ref_ids` 只导出本人记录、指定他人 weld JSON/PDF 均 403；未知 type / format → 400（40000）；引用实体不存在（validation 999999 / data-list 未知 weld）→ 404（40401）；未登录 → 401（40100）。
- `test_common.py`（Task 3）：`ok/err/paginate` 信封纯函数 + 全局异常处理器（隔离迷你 app）+ `write_audit` 审计写入；**Task 5 P2 回归**：长 `resource_id` 仅在 >255 时截断并附摘要，`detail` 递归脱敏。
- `test_storage.py`（Task 4）：`normalize_filename`/`normalize_key` 纯函数断言 + 假 `Minio` 客户端注入测参数透传，不连真实 MinIO。
- `test_signal_ingest_iso_time.py`（Task 18 修复，纯函数不连 DB/存储）：`_time_column_to_seconds` 对 ISO 8601 时间戳（`YYYY-MM-DDTHH:MM:SS.ffffffZ`，200µs 递增）解析为相对秒 + 数值时间戳不回归；`validate_signal` 用 ISO 时间列 → R6 采样一致性 pass、fs≈5000、duration>0、overall 非 fail（真实 CSV `data/data/SP2026-06-000201/SP2026-06-000201.csv` 910000 行端到端）；`detect_events` 的 events/anomalies 坐标非 NaN 有效数值；`to_parquet_bytes` t 列无 NaN。修复背景：原 `pd.to_numeric` 无法解析 ISO 字符串 → 全 NaN → 「时间列无有效递增差值」fail。**2026-08-29 追加**：`GasSpeed`/`gas_speed`/`gasflowspeed` 补入 gas 别名后，真实 CSV 的 `GasSpeed` 不再进未知列，新增 `test_map_columns_gas_speed_aliases` 断言映射（背景：该 CSV 的 `GasSpeed` 是唯一有值通道，此前被当未知列丢弃导致预览全 0）。
- `test_signal_ingest_events.py`（**2026-09-15 焊接段提取回归**，纯函数不连 DB/存储）：`detect_events`
  对带短路过渡纹波（每 50ms 插入 4ms 陷波）的信号必须把焊接段还原成完整区间（修复前被切成
  265 段、最长 0.142s，页面「有效焊接段」显示 0.13s）；`_merge_runs` 对间隔 ≥ 容差的段不合并
  （两条焊缝不能粘成一条）。
- `test_signal_ingest_extra_columns.py`（**2026-09 多模态字段扩展**，纯函数不连 DB/存储）：`map_columns` 对 16 列标准头全识别（WireFeedSpeed→wir、WeldingSpeed→weld_speed、j1..j6、width/height/square/perimeter→pool_*）；`validate_signal` 对可数值未知列自动保留、非数值列仍 unknown（R2 warn）；`to_parquet_bytes`→`bundle_from_parquet` 动态列往返（核心 4 恒在 + 扩展通道，量程按数据实际 min/max）；`build_field_summary`/`fill_record_params` 字段概览与单值工艺列回填。
- `test_labelstudio_integration.py`（**2026-09-06 LS 集成回归**，不连真实 LS/MySQL/MinIO）：纯函数段——`convert_region` 对 `rectanglelabels`/`polygonlabels` 的 **0-100%→像素**（original_width/height）换算、`_region_category`（`value.<plurallabel>[0]` 兼容 labels）、`extract_annotator`（completed_by.email/id / created_username）、`media_field_for`；`handle_annotation_event` 段——内存 SQLite + monkeypatch `labelstudio._client` 返回假 client（tasks.get 返回假 annotation），验证回写 `annotations`（category/kind/box/annotator）+ sync/task 状态 `synced` + **幂等**（重发不重复落行）+ 无有效标注（was_cancelled）→ 不写；`push_samples_to_ls` 在 `_client=None`（off/不可达）时返回 0 不抛（best-effort）；**任务完成语义（决策 2）**——单样本全回写 → job `succeeded`；多样本仅一样本回写 → 任务仍 `pending_ls`、job 未终态。真实全链路走 `scripts/premise_validation/e2e_ls_roundtrip.py`。
- `test_labelstudio_handler.py`（**2026-09-06 LS 接线回归**，不连真实 LS/MySQL/MinIO）：内存 SQLite + monkeypatch `labelstudio._client`（假 client 记录 `tasks.create`）+ `app.storage.get_storage`（假 presign）。覆盖 `app.jobs.annotation.handle` 在 **mode=on** 时把样本推 LS + 置 `ls_status=pending_ls` + 建 `annotation_ls_sync` + **job 不立即 succeeded**（等回写）；**mode=off** 回退 `simulate_annotation`（job succeeded + 无 sync 行 + 未进等待态）；**mode=on 但 LS client 不可达** → 回退模拟不炸（best-effort）。
- `test_auth.py`（Task 5）：JWT 认证。内存 SQLite **必须 `poolclass=StaticPool` + `check_same_thread=False`**——TestClient 在 worker 线程处理请求，默认 per-thread 池会让各线程看到互相独立的空库（"no such table"）；用 `app.dependency_overrides[get_session]` 覆盖登录与 `get_current_user` 两处依赖，seed 一个 argon2 用户测 login 成功/密码错/用户不存在、me 带/不带/坏 token、`decode_token` 往返、`verify_password`。另含防枚举机制断言：`_DUMMY_HASH` 是合法 argon2 哈希且任意明文均不匹配（不直接断言耗时，避免 CI 抖动）。**Task 4 修复回归**：连续失败登录达到阈值后返回 `42900`，冷却结束后正确密码恢复可登录。
- `test_seed.py`（Task 6）：内存 SQLite + `SQLModel.metadata.create_all`，同线程直跑 `seed_all`（不用 TestClient/StaticPool）。`seed_all` 两次幂等（各表数量不翻倍）；label_categories==6（含熔池，LS 决策 4）/ welds==4 / models==3 / datasets==3；管理员 `verify_password(admin_password)` True；0248 有 4 版本且 latest→v1.3、核验 93.3/14/1/0/2.8 + 15 条规则（第 9 项警告）；0245 停在 v1.0；标注页演示数据就位。
- `test_jobs.py`（Task 7）：通用 Job 服务 + `GET /jobs/{job_id}`。内存 SQLite + StaticPool + 真实 app TestClient（同 test_auth.py）；依赖覆盖 `get_session` → 测试 session，另 override `get_current_user` → 假 User（免 seed/签 token）。覆盖：create_job（job_uid 前缀 `job_` + 8 位、pending/progress=0、created_at UTC aware、**不 commit**——rollback 后查不到、flush 即分配 id）、状态机（running/succeeded/failed 各设字段 + finished_at）、to_job_payload §1.5 形状 + ISO-8601 `...Z` 时间、`_iso_utc` 对 **naive datetime 按 UTC 处理**（读回路径 tzinfo 被剥离，防系统时区偏移）、GET 200 信封 / 未知 uid 404（40401）/ 未登录 401（40100）。
- `test_dashboard.py`（Task 8）：Dashboard 四端点。内存 SQLite + StaticPool + 真实 app TestClient；`seed_all` 造演示数据（4 焊缝 / 3 数据集 / 2 样本 / 2 标注）后 override `get_session` + `get_current_user`。覆盖：四端点信封 code==0、stats 数值（data_total==4 / manufacturer_total==4 / max_storage_bytes==2576980378 / annotated_samples==2 / completion==100.0）、attributes 非空（weld_methods 4 台 distinct machine、defect_types 含统计口径词表、modalities/sample_rate_tiers）、distributions 形状（welding_types==MAG2/MIG1/TIG1、defects 含全部统计词表、wordcloud name/size）、projects 3 条且字段来自数据集、四端点未登录全 401（40100）。
- `test_files.py`（Task 9）：files 三端点。内存 SQLite + StaticPool + 真实 app TestClient；override `get_session` + `get_current_user`（假 User），**monkeypatch `app.api.v1.files.get_storage`** → 包 `FakeMinio` 的 `StorageClient`（只做参数透传断言，不连真实 MinIO）。覆盖：presign-upload 有效（object_key 以 prefix 开头 + 默认文件名 `file`，非 `uploads/` 前缀 `lifecycle=None`）/自定义 filename（中文主干回退 `file.mp4`）/size=0 与 size>2GB→400/空 prefix→400；**Task 4 修复回归**：presign prefix 与 get url 均拒绝绝对路径/反斜杠/`..` 路径穿越；upload 小文件 200（upload_stream 记录 key+size=11、GET url 已签发，**返回 `lifecycle={temporary,30天,uploads/}`**）/**monkeypatch `MAX_PROXY_UPLOAD_SIZE`=4 发 5 字节→400 且未触发真实上传**；**Task 5 P2 回归**：默认 5MB 以上 CSV 直接 `40000` 提示改走 `presign-upload`（不再静默长超时），长文件名上传不因审计 500；get url 正常/`?expires=86400` 边界/expires=999999→400/空白 key `%20`→400（Starlette 会对 path 参数 unquote，strip 后为空）；三个端点未登录全 401（40100）。
- `test_welds.py`（Task 10）：Welds 核心 CRUD。内存 SQLite + StaticPool + 真实 app TestClient；`seed_all` 演示数据（4 焊缝 / 0248 v1.0~v1.3 / 0248 已核验 93.3）后 override `get_session` + `get_current_user`。覆盖：登记事务（WLD-/REG- 编号生成 + v1.0 + latest 联动 + 同日两次登记序号递增 0001/0002、source 为空 400；**Task 5 P2 回归**：受控并发同 payload 双提交返回 `200+409`，仅落一条记录；**Task 5 修复轮次 1**：真实 MySQL 临时库复现 `GET_LOCK` 驻留——rollback 后原连接先被别的 Session 抢走，再调用释放逻辑，后续普通登记必须仍 `200`，防回归 `40900 获取登记编号锁失败`）；`GET /welds` 分页/筛选（q/source/brand/status/tab=待核验·已归档、组合）；版本链（0248 4 版 + `POST …/versions` 加 v1.4 并 bump latest、**相同 action/note/object_keys 重复版本 40900**、非法 action 400、跨焊缝版本 40402）；raw-files（追加 keys + storage_bytes 累加 + modalities 推导、空 keys 400、登记编号作标识；**Task 4 修复回归**：缺失对象不落库/不计容量，重复 key 幂等不重复累计；**Task 5 P2 回归**：若请求中的 CSV 已存在 `signal_ingests` 行，则返回 409 且 storage_bytes 不重复累计）；PATCH/GET registration（partial update 未提交字段保留、id/registration_no/weld_id 三种标识、不存在 404）；核验（0248 v1.2 对齐版 15 全过→quality 通过 + 报告字段；新建无文件版→failed>0→quality 异常；**0248 v1.0 raw 视频完整版 15 全过→quality 通过——回归保护"raw/ 前缀不再触发帧率警告"，否则上传数据永远待复核**）；10 个端点未登录全 401（40100）。
- `test_dsp.py`（Task 11）：DSP 纯函数（不连 DB）。低通对白噪声高频能量比断言（滤波后 < 原始 ×0.5）；带通保留通带；**归一化截止频率相对奈奎斯特 fs/2**（fs=5000 时 0.02 ↔ 50 Hz；同一物理 Hz 在 5k/20k 采样率下结果一致；低通/高通/带通通带边界按 rms 断言，2026-09-15）；50Hz 正弦 PSD 峰在 50Hz±10；DWT D1..D4+A4（5 项）；小波 5 层 L1..L5；PDD counts 之和 == 样本数；STFT magnitude 形状 (freqs×times)；相图降采样 ≤2048 且短信号不降。
- `test_analysis_filter.py`（2026-09-15）：滤波口径回归（不连 DB，直接 import `app.api.v1.analysis`）。`_series_range` 对高通去均值信号给跨 0 量程、均值与数据一致、常值/空序列不产生 inf/nan；`_filter_error` 拒绝越界归一化频率/倒置带通/未知类型。
- `test_analysis.py`（Task 11）：Analysis 端点。内存 SQLite + StaticPool + 真实 app TestClient + `seed_all` + override `get_session`/`get_current_user`。覆盖：candidates 只含通过焊缝（0248/0246）；signals 4 通道 + events/anomalies + 时长 5.42 + `channels[]` 筛选 + 滤波联动（值仍在量程）+ 非法滤波参数/未知通道 400 + 40401/40402；六 mode 参数化返回 200 且含预期键；result 有 stability/segments/anomalies 且确定性可复现；未知 mode/通道 400；未登录全 401（40100）。坑：`compute_psd` 返回 dict，测试需按 key 取 `freqs/psd`（tuple 解包会拿到 dict 的 key 字符串）。
- `test_features.py`（Task 12）：特征提取（纯函数 + 端点）。纯函数段：`ts_features` 8 键 + 均值≈np.mean + 50Hz 正弦 FFT 主频≈50Hz；`vision_features` 8 键 + 面积>0；`audio_features` 6 键 + 全有限 + 全零输入安全；`unify` total_dims==42 + groups 覆盖 [0,42) + Z-Score 方差≈1 + Min-Max∈[0,1] + L2 范数≈1 + `无` 原样透传 + format 透传 + 未知归一化抛 ValueError。端点段（内存 SQLite + StaticPool + 真实 app TestClient + `seed_all` + override 依赖）：POST `/features/extract` 200 + 形状对齐 App.tsx + 落库（直查 `feature_extractions` 行）+ GET 往返同一份；**缺模态版本返回 `modality_status`（timeseries available、vision/audio fallback）**；坏 weld/version/跨焊缝版本 → 40401/40402；非法 normalization/format → 400；未知 extraction_id → 40401；未登录 401。
- `test_alignment.py`（Task 13，对齐真实化后为真实内核测试）：Job 执行器 + 对齐任务。内存 SQLite + StaticPool + 真实 app TestClient + `seed_all`；override `get_session`（每请求新 Session）/`get_current_user`，并 **monkeypatch `app.jobs.executor.SessionLocal` 到同一测试引擎**（`run_job` 用独立 session，不启动轮询线程）。覆盖：POST 对齐任务 → `{job_id}` + pending；`run_job` 后 status=succeeded + result 内嵌 events/event_source/tracks/assets/version + **自动多出 v1.4「时间对齐」版本且 latest_version_id 指向它**；**generated 回退路径**（seed 无真实信号/视频）：event_source=generated、video 轨道 unavailable + reason、assets 为真实 timeseries CSV/tracks.json（无 .mp4/.wav/.avi 占位）且 tracks.json 结构与 result 一致；**真实信号路径**（`FakeStorageWithRead` 带读缓存 + 手工造 succeeded SignalIngest + `to_parquet_bytes`）：event_source=real、events == ingest.events、timeseries_weld.csv 行全部落在焊接段内；**真实视频路径**（imageio-ffmpeg 生成 2s/10fps mp4 上传到 seed v1.0 真实对象键 `raw/REG-20260815-00248/0001.mp4`）：video 轨道 available、fps/宽高/时长真实探测、超出时长的事件关键帧跳过、keyframes 真实上传 JPEG（SOI 开头）；**重复对齐回归**（目标版本 object_keys 附加上次 `/align/` 产物 keyframes jpg/align CSV/tracks.json）：不再回灌为原始模态源——红外轨道 unavailable、timeseries 源仍是 raw CSV；**存储写失败时删除已写对象（顺序无关断言）且不落库虚假 assets/版本**；**缺少视频/时序输入 40000**；**同 version 的 pending/running/succeeded 重复提交返回既有 job_id（幂等）**，failed 后再次提交会创建新的可执行 job，随后重复点击仍返回该新 job_id；并含文件库 SQLite 真并发双请求回归；monkeypatch `HANDLERS["alignment"]` 抛异常 → job failed + error 落库（不滞留 running）；weld/version 缺失 → 40401/40402、未知 task_id → 40401；未登录 401（40100）。坑：GET 对齐任务在未执行/失败时保持 result=null（域字段仅在 succeeded 后合并）；真实视频用例依赖 imageio-ffmpeg 二进制（同 gen_destructive_data）。
- `test_media_probe.py`（对齐真实化新增，不连 DB）：`parse_ffmpeg_info` 纯函数（Duration/fps/WxH 解析 + 缺字段 None）；`analyze_video` 对 imageio-ffmpeg 生成的 2s/10fps/320x240 mp4 端到端探测（duration/fps/宽高近似断言）；空数据/伪类型文本 → ValueError；超出视频时长的事件跳过（EOF 附近 `-ss` 抽不到帧）、负时刻跳过；`get_ffmpeg_exe` 在 imageio_ffmpeg 导入失败（monkeypatch `sys.modules`）时抛 RuntimeError。
- `test_alignment_mapping.py`（**2026-09-22 多模态统一坐标系，不连 DB**）：`alignment` 的坐标映射纯函数。覆盖 `position_ratio_at` 插值 + 区间外**钳制不外推**；`arc_length_profile` 三级降级（`channel` 恒速时退化为时间比例 / `channel` 变速时**必须偏离**时间比例——中点 `≈9.8/15` 而非 0.5 / 负速度截零不计入 / 无通道退回 `scalar`→`none` 且两者折线相同 / 全程零速不除零）；`_normalize_roi` 形状校验（w/h ≤ 0、字段缺失、非数值全拒）；`build_coordinate_mapping` 结构（`identity` 时序恒对齐、**未标定 offset 时视频轨 `calibrated=false` 且 reason 含"未标定"并把 offset 落 0 供换算**、标定后带 offset、无视频文件时 `offset_seconds=None`、焊缝图片 `calibrated` 由 ROI 决定、`speed_source=none` 时仍产出可用的纯比例折线、统一轴/事件边界取自 bundle）；末尾断言映射可 `json.dumps`（**不得带 numpy 标量**，它要写 JSON 列与 `mapping.json`）。
- `test_calibration_api.py`（**2026-09-22 标定 API + 对齐 offset 消费，E2E**）：内存 SQLite + StaticPool + 真实 TestClient + 依赖覆盖 + `app.storage.get_storage` → 内存假存储；视频用 imageio-ffmpeg 现生成，信号用**真实导入**（`SignalIngest` + `run_ingest` + Parquet——`load_signal_bundle` 已无 generated 回退，缺导入直接抛）。覆盖 `GET`/`PUT …/calibration` 往返、**offset 正负**、未标定默认值、**ROI 形状/越界/图片不可读/无图片一律 400 且不落半成品**、**任何版本写入都锚定 v1.0**（v1.1 自身 `calibration` 仍为 NULL 但读到 v1.0 那份）、鉴权 401/403/404、`update/calibration` 审计落库（含 detail）、**对齐任务写 `mapping` 且 `aligned` 随 `calibrated` 变化**、**换 offset 换一批关键帧**（人造信号事件固定在信号轴 0.59/2.61，视频 2s，offset=0 抽到早事件、offset=1.0 抽到晚事件）、**重新标定不改历史 `alignment_tasks.mapping` 与已落盘的 `mapping.json`**。**坑**：`run_ingest` 与 `run_alignment` 都**不自己 commit**（executor 职责），测试须自行 `db_session.commit()`，否则断言看到的是回滚后的旧值。
- `test_seam_image_excluded.py`（**2026-09-23 焊缝图片「不参与分段」，不连远程库**）：内存 SQLite +
  StaticPool + 真实 TestClient + 依赖覆盖 + 假 Storage（**记录被读过的对象键**）。覆盖
  `PUT …/calibration` 的 `{seam_image:{excluded:true}}` 写入（**不下载图片**，`downloads == []`）、
  给 `{roi}` 撤销不参与（显式回写 `excluded:false`）、`excluded` 覆盖旧 ROI、`{seam_image:null}`
  清回"未标定"（与"不参与"区分）、非布尔 400；`build_coordinate_mapping` 的
  `available=true`+`excluded=true`+`calibrated=false`+`SEAM_EXCLUDED_REASON`；**未框选与不参与
  的原因必须不同**；样本 manifest（`map_window_to_modalities`）记 `available=false` + `excluded=true`
  且无 `spatial_range`；预览 warnings 在已明确不参与时**不再催去标定**、`_preview_modalities`
  透出 `excluded`；`_seam_image_track` 不声称 `aligned`。
- **逐段视频代表帧（2026-09-23）**：`test_splitting_v3.py` 增 4 条纯函数用例——代表帧 = 窗口中点
  （信号轴）且 `t_video = t_signal - offset`、帧号 = `t_video × fps`；中点落在视频覆盖范围外
  **返回 None**（不钳端点）；视频不可用/无帧率 → 没有代表帧（不猜时刻）；不可用分支也带
  `frame.reason`。`test_split_v3_api.py` 增 5 条 E2E——每段拿到**自己的**真帧（对象键互不相同、
  落盘字节是真 JPEG、JSON 里只有短期 URL 无二进制）、**预览与 Job 的帧逐字段一致**
  （`t_signal`/`t_video`/`frame_no` + 产物键在 `object_keys` 里且为 `.frame.jpg`）、视频对象缺失时
  逐段给"抽帧失败"原因、**段数不设上限**（造 26 段——正好越过已删除的旧上限 `PREVIEW_FRAME_LIMIT = 24`
  ——每段都必须有自己的帧，回归"第 25 段起没有代表帧"）、窗口落在覆盖范围外时
  "整段不可用"与"仅中点越界"两种原因分开。该文件另加**逐用例清 `splitting._PREVIEW_CACHE`**
  的 autouse fixture（缓存键含 version_id，各用例的库都是新的、id 从 1 重来 → 会跨用例误命中）。
  **代表帧复用（2026-09-23）**：`test_preview_reuses_stored_frames_instead_of_rerunning_ffmpeg`
  ——包一层 `media_probe.analyze_video` 与 `storage.get_object` 计数，清掉 `_PREVIEW_CACHE` 后
  再预览一次，断言 ffmpeg **没被再调用**、视频**没被再下载**、对象键一致且 URL 是新的。
  关掉 `_object_exists` 的复用判定这条就会失败（`assert [3, 3] == [3]`），是它存在的意义。
- `test_splitting_v3.py`（**2026-09-22 分段 v3 纯函数，不连 DB**）：`build_time_windows`（默认 2/2、重叠、尾片 `drop`/`keep`、**刚好整除时 `keep` 不多出零长尾片**、`ceil` 派生采样点、非法输入报错）；`map_window_to_modalities`（offset **正负号**、整段落在视频外记 `available=false` 不伪造帧号、部分重叠钳到 0、首/中/末帧只留覆盖范围内的、ROI 弧长投影、无 ROI 不可用带原因、manifest 的 `schema_version`/`source.mapping_hash`）；`preview_token`（往返、**改一字节即失效**、过期、格式错）；`rules_hash`/`mapping_hash` 稳定性与内容敏感性。
- `test_sample_annotation.py`（**2026-09-22 分段样本段级标注**）：服务层 + `/split-tasks/{id}/annotation-samples` 端点。
  内存 SQLite + StaticPool + 真实 TestClient + 依赖覆盖 + 假 Storage + `executor.SessionLocal` 指到测试引擎。
  覆盖 ① **关键流程端到端**（列表 → 未标注筛选 → 保存正常/缺陷 → 进度与缺陷分布 → 撤销 → 版本化导出）；
  ② 结论约束（defect 必须给类别、normal 必须不带、**别的分组的字典 id 不算缺陷类别**、
  **停用类别挡新写入但放行"本来就引用了它"的旧标注**、备注长度与空白归一、upsert 不产生第二行）；
  ③ 前置条件（非 v3 / 未成功的分段任务 400、跨任务样本 404、撤销不存在的标注 404、非法 filter 400、未登录 401）；
  ④ 词表管理走 `/settings/options/defect_category`（新增/改名/上下移/**被引用删除 → 降级停用**且标注行仍可读）；
  ⑤ **数据集构建消费**（跑真实 `dataset_build` job → `dataset_items.annotations` 冻结快照含段级条目 +
  `empty_label_rate` 与快照同口径）；⑥ 训练折叠优先认 `label`、旧标注线并存不被改写。
  **坑**：`_seed_split_task` 必须给不同 `suffix`（`weld_id`/`registration_no` 唯一）；夹具内自带
  `seed_reference_data`（词表是"能标注"的前提）。
- **删除分段任务（2026-09-23，在 `test_split_v3_api.py` 内）**：4 条 E2E——① 删成功
  （样本/标注/job 行都没了、产物对象也真删了、`GET` 变 40401、任务列表不再列出）；
  ② 样本已进 `dataset_items` 固定快照 → `40900` 且**一行都不删**；③ 任务还在 `running`
  → `40900`；④ 未知任务 40401、未登录 401。
  **⚠️ 该文件的 engine 夹具开了 `PRAGMA foreign_keys=ON`（`event.listens_for(engine, "connect")`）
  —— 别删掉。** SQLite 默认不校验外键，级联删除的先后顺序错了也全绿；`SampleAnnotation` 与
  `Sample` 之间只有裸外键列、没有 `relationship()`，SQLAlchemy 推不出先后，把 `samples` 的
  DELETE 排在了 `sample_annotations` 前面——离线全绿、MySQL 线上 `1451` 500。这条 PRAGMA
  就是那次事故的守卫（去掉它，用例仍会全绿，但线上会炸）。
- **标注总览时间轴（2026-09-23，在 `test_split_v3_api.py` 内）**：3 条 E2E 挂在既有的
  `ready` / `_run_split` 夹具上（复用真实信号导入 + 真视频抽帧 + 真图片裁切，不另起一套）。
  ① 一次拿全 —— 窗口来自**已落库的 `Sample`** 且按时间有序、标注态逐段回填、每段的帧/切片 URL
  互不相同且指向 `meta` 里的类型化键（不按 `object_keys` 后缀猜）、`timeline` 与 `split-preview`
  逐字段一致（**同一个 `build_timeline_layers`**）、完整 manifest 不进响应；
  ② **波形读不回来不阻断标注** —— monkeypatch `splitting.load_input` 抛 `SplitInputError` →
  `timeline=null` + `warnings` 写明原因，但窗口、标注态与媒体 URL 照常返回；
  ③ 段数超 `MAX_TIMELINE_WINDOWS` → `40000` 且**不静默截断**。
- `test_split_v3_api.py`（**2026-09-22 分段 v3 端点 + Job E2E**）：内存 SQLite + StaticPool + 真实 TestClient + `app.storage.get_storage` **与 `app.jobs.split.get_storage` 双 patch**（模块级导入的绑定要单独 patch，同 `test_dataset_build_e2e`）+ `executor.SessionLocal` 指到测试引擎 + 真实信号导入。覆盖预览契约（默认 2/2、token、分层 timeline 点数上限、模态可用性与 `calibrated`、warnings、**只读不建任务**、非法窗口 400）、创建**只吃 token**、跨版本 token 拒绝、**标定变更后 409**、有效区间变更 409、样本分页与详情（列表不含 `meta`、详情含 ≤600 点局部时序、404）、`get_split_task` 暴露 `rules_version`/`schema_version`、**Job 写多模态 meta + 时间列 + manifest + 图片切片**（裁出的 JPEG 高度等于 ROI 高）、**图片裁切失败不阻断任务**。**坑**：视频元信息来自**对齐产物**（分段侧不下载探测），所以测试要在 `ready` 里先 `_seed_alignment_mapping` 造一次成功对齐的 mapping，否则视频模态恒不可用、标定 offset 进不了映射（409 用例也测不出来）。
- `test_split_annotation.py`（Task 14）：切分 + 标注（模拟）。同 test_alignment 基础设施（内存 SQLite + StaticPool + `seed_all` + override 依赖 + executor SessionLocal 指向测试引擎）。覆盖：切分创建 → `run_job` → succeeded + `SplitTask.sample_count` 回填，并**真实把 result.samples 对应的 JPG/JSON 写入** `processed/{weld_id}/split/`；`samples` 行 object_keys 前缀正确，result 内嵌 `samples` 仅前 50 条预览；**存储失败时删除已写对象并保持 DB/Job 一致**；**缺少时序输入 40000**；**同 version+rules+task_format 的 pending/running/succeeded 重复提交返回既有 job_id（幂等）**，failed 后再次提交会创建新的可执行 job，随后重复点击仍返回该新 job_id；并含文件库 SQLite 真并发双请求回归；切分非法参数/未知焊缝版本/未知 task_id；标注从切分任务创建 → `run_job` → 样本 `annotation_task_id` 指向新任务（samples_count==切分样本数）、manual 不归属；import files 建样本 / split_task 改归属；label-categories==5；AI 预标注**确定性**（同样本两次内容一致，比 category/box/confidence 不比 id——每次"删旧插新" id 会变）+ annotator=AI预标注 + 样本级 confidence=均值；save labels 覆盖写（annotator=林工）+ **confidence 语义**（缺省沿用预标注同类别值 / 显式覆盖 + 均值更新）；未知类别/坏 box → 400；**confidence 越界（≥10 / <0）→ 400 而非 500**（review 修复，边界 0/1 仍 200）；样本分页（page_size 上限 100 钳制）；404（未知任务/样本/跨任务样本）+ 401（10 个端点全验证）；monkeypatch `HANDLERS["split"]`/`["annotation"]` 抛异常 → job failed。
- `test_datasets.py`（Task 15）：数据集 + 构建任务 + dimensions/readiness/lineage。同 test_split_annotation 基础设施 + **monkeypatch `app.storage.get_storage` → FakeStorage**（记录快照写，不连 MinIO）。覆盖：列表 3 条 seed；新建（dataset_no `DS-{类别}-{全局序号}`、状态 标注中、sample_count 0、同名 → 40900、空名 → 400）；dimensions 7 项 + 各任务必需维度 + 状态枚举；readiness 形状；**详情新增 `label_distribution`**；版本新建 → 版本号递增（v1.1 → v1.2）+ 详情；**版本成员分页接口**（items 至少含 weld/sample 字段，`total` 与页切片一致，真实存在但属于别的数据集的 version_id 返回 `40402`，未登录 401，关系外键路径可 q/quality/split 筛选且源码回归断言过滤不含 `samples.meta` JSON path）；**re-review 回归**：meta-only 手工样本可在 SQL 中按 q/quality 命中，紧凑、pretty-print（space/tab/LF/CR）及引号 `record_id` JSON 形态都可匹配，并以 `record_id=12` 对 `123` 的相邻多位 ID 断言不误配；版本成员页内 payload helper 能把 JSON 字符串解回 `meta/modalities`、优先 meta 解析出的记录字段，并在缺失值时回落到 `[]/None`，覆盖 SQLite/MySQL 类型解码差异风险；构建任务（**空来源 → 兜底合成样本**，覆盖 4 焊缝 → split 全三类 + **同焊缝样本不跨 split（防泄漏）** + quality 含 repeat_rate/empty_label_rate/dimension_missing_rate + snapshot_id 落库 + 假存储收到快照 JSON + dataset.current_version_id/sample_count/status 可训练更新）；构建任务（**真实切分样本**：单焊缝 → 全进 train 不泄漏）；**固定快照回归**：新版本重建后旧版本详情与 snapshot 仍保持不变；另含 **后台 executor 自动消费 dataset_build**；lineage 4 层节点（records/annotation_tasks/dataset_versions/training_tasks）+ 未构建仍 4 层；404（数据集/版本 40401、版本不属于数据集 40402、非法来源 40000）+ 401（10 端点全验证）+ monkeypatch `HANDLERS["dataset_build"]` 抛异常 → job failed。
- `test_audit_routes.py`（Task 4）：upload/presign/alignment/features/report export 审计覆盖回归；确保 `audit_logs` 中存在 `upload/file`、`presign_upload/file`、`create/alignment_task`、`extract/feature_extraction`、`export/report`。
- `test_authz.py`（Task 4）：ownership/ACL 回归；普通用户不能读取/修改管理员登记及其焊缝/版本/analysis/features/report 关联资源，创建者仍可访问自己的登记，**同名 display_name 不会串权、owner 改名后仍能访问**，管理员路径不回归。

- `test_settings.py`（**2026-09 系统设置·可选项字典**）：服务层（内存 SQLite 直接调 `services.settings`）+
  端点层（StaticPool + 真实 TestClient + override `get_session`/`get_current_user`）。覆盖：
  6 组齐全且出厂默认值与字典化前硬编码一致（machine/weld_method/dataset_task；`product` 出厂为空）；
  重名 409 / 未知分组 40410 / 空值 40000；改名、停用-启用往返（停用项仍在设置页可见）；
  上移下移**整组重排 sort_order**（含边界静默不动作、非法 direction 400）；
  **删除语义**（未被引用 → 物理删 `mode=deleted`；被 `data_records.machine`/`datasets.task`/
  `annotations.category` 引用 → 软删 `mode=deactivated` 且历史值保留）；
  `label_category` 落在 `label_categories` 表（不搬家）+ `GET /label-categories` 带 `active` +
  **AI 预标注只抽启用类别**；写操作非管理员 403（40300）、未登录读 401（40100）。
- `test_dashboard_transition.py`（**2026-09-15 / S2**，内存 SQLite 直接调 `services.dashboard`）：总览「过渡类型」映射回归——已知焊法（MAG焊=短路过渡 / 埋弧焊=脉冲过渡）按映射归位；登记页放开自定义输入后，映射表外的焊法（如「激光-电弧复合焊」）统一计入 `UNKNOWN_TRANSITION = "未分类"`，**不再被静默算成「脉冲过渡」**（旧兜底会给出看起来正常、实则错误的结论），且原始焊法仍出现在 `welding_types`（不丢原文）。

## 坑/限制

- **内存 SQLite + TestClient 必须 StaticPool**（见上）。`test_models.py` 未用 TestClient、同线程跑，才可用默认池。
- 断言 `err()` 信封用 `json.loads(resp.body)`（`err` 返回 `JSONResponse`，无 `.json()`）。
- 异常处理器测试用独立迷你 FastAPI app，避免污染真实 `app.main` 的路由/中间件。
- 依赖覆盖用函数对象做 key：`app.dependency_overrides[get_session] = override`，用后 `pop` 恢复。
