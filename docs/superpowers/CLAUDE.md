# CLAUDE.md — docs/superpowers/

superpowers 工作流产物目录。

- `plans/`：实现计划（含 `2026-08-23-ai-welding-fullstack.md`，SDD 执行中作为任务拆分来源）。

坑/限制：
- 本目录文件为开发工作流产物，非业务契约；业务契约仍在 `docs/` 根（API接口清单/数据库设计/OSS存储设计/开发规范）。
- 计划执行状态见 `<repo-root>/.superpowers/sdd/`（git-ignored）下的 ledger。
