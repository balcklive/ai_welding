# CLAUDE.md — docs/superpowers/

superpowers 工作流产物目录。

- `plans/`：实现计划。已完成的计划（2026-08-23 全栈 / 版本抽屉 / 功能验证 / docker-cicd / 数据集概览 / 标注 kinds / 工具栏修复 / 数据集层级）已删除归档，设计存档见 `specs/`；当前仅剩待执行的 `2026-08-29-dataset-management-reference-tasks.md`。
- `specs/`：已确认的设计规格（对应各已完成计划的方案存档）。

坑/限制：
- 本目录文件为开发工作流产物，非业务契约；业务契约仍在 `docs/` 根（API接口清单/数据库设计/OSS存储设计/开发规范）。
- 计划执行状态以对应实现代码为准；`<repo-root>/.superpowers/sdd/` 为 git-ignored ledger，当前不存在（无活跃 SDD 会话）。
