# CLAUDE.md — src/

前端源码（React 18 + TypeScript + Tailwind）。

- `main.tsx`：React 入口，挂载 `App`。
- `App.tsx`：主应用。**2026-08-29 重构为路由组合**——`AppShell` 渲染侧边栏（导航树 `app/navigation.ts`）并维护全局 `selectedDatasetId`/`selectedDataId`「先选数据」上下文；`WorkspaceFrame` 按 `route` 经 `React.lazy` 懒加载各 `features/*` 工作区（overview / datasets / registration / validation / versions / analysis / annotation / alignment / models / features），`Suspense` 包裹。**以下段落是页面接 API 的历史接线笔记，对应组件已迁入 `features/*`，具体职责与坑见各子目录 `CLAUDE.md`。** 数据详情中的 `RawSignalPreview`（现 `features/datasets`）复用 `/signals` 接口展示真实多通道原始波形，失败时明确回落演示波形。数据集列表不显示全局选中数据上下文条；点击侧边栏“数据中心/数据集”可从任意数据集子页面返回数据集列表。版本列表的“查看”打开右侧 `VersionDetailDrawer`：焊缝版本读取版本详情与核验结果，数据集版本读取固定快照详情。**Task 21 起总览页与数据列表已接 API**（`Overview` ← `getDashboardData`，内部聚合调用 `getStats/getAttributes/getDistributions/getProjects` 并使用 5 分钟浏览器缓存；`ManagementFiltered` ← `listWelds` 服务端筛选分页；`SelectionContext` ← `getWeld`）；**Task 22 起数据中心其余页已接 API**——`Registration`（页面改名「数据上传」，原「数据登记」；登记先选所属数据集 ← `listDatasets`（默认取第一个），提交 ← `createRegistration`（`dataset_id` 必填，缺失/为空则禁用提交），**文件上传拆 4 个独立区**（时序 CSV/图片/视频/WAV，`UPLOAD_ZONES` 配置：各带 `accept` + `zoneAccepts` 类型校验拦截、独立上传状态与文件 input，文件键按区暂存 `pendingKeysRef` → 提交成功统一 `attachRawFiles(id, keys)`；**提交要求至少一个文件**，`hasFile` state 驱动按钮启用）、上传 ← `uploadFile`(<100MB)/`presignUpload`(≥100MB)+PUT（**PUT 后先查 `res.ok`，失败抛错丢弃 object_key**），最近上传 ← `listWelds({tab:'recent'})`，**采集时间可编辑**：`datetime-local` 输入（默认当前本地时间，`toLocalInputValue` 生成 `YYYY-MM-DDTHH:mm`，Pydantic 可直接解析），此前误设 `readOnly` 写死 seed 时间；缺省纳入 `missingFields`（5 个启用条件 dataset/source/collected_at/weld_name/hasFile）；回调读 `regIdRef`/`pendingKeysRef` 修 stale-closure 竞态、上传失败 `regError`、file input 重选清空；**上传时序重构（延迟上传）**：选择文件只**锚定**（`files` state 存 File，状态 `pending`「已选择（待上传）」，不发网络请求），点「上传数据」才提交——`submitting` 期间按钮禁用态，流程 = `createRegistration`（部分失败重试复用 `regRef` 防重复登记）→ 逐文件**预签名直传**（`presignUpload` + `putFileDirect` XHR 带进度，状态 `uploading` 显示「上传中：文件名 x%」；prefix 固定 `raw/`，**勿用 `uploads/`**——有 30 天生命周期清理）→ `attachRawFiles` 统一挂载；成功按钮变「上传成功：REG-...」；**必填项缺失 UX**：带 * 星标红色（`.required-mark`），4 个启用条件（dataset/source/weld_name/hasFile）由 `missingFields` 统一驱动——按钮禁用态不再用原生 `disabled` 而是样式类 `full-button--disabled` + `aria-disabled`（可接收点击），点击列出缺失项提示（`missingHint`，红字 `.toolbar-error`）并对对应输入 label / 上传区红色闪烁 1.2s（`flash` state + `.field-flash` 动画，`flashTimer` 防重入））、`Validation`（挂载 ← `getWeld` 取 `latest_version_id` → `getValidation`，执行 ← `runValidation`）、`VersionPanel`（只读版本链 ← `listVersions`）、数据集（入口为 `data-center/datasets`；`DatasetWorkspace` 按**数据集列表 → 数据集概览 → 当前版本成员 → 成员详情**层级展示，无独立全局数据列表导航：列表/新建 ← `listDatasets`/`createDataset`，概览 ← `getDataset`+`getDimensions`+`getReadiness`+`listDatasetVersions`+`getLineage`，成员 ← `listDatasetVersionItems` 服务端分页/筛选，新建版本 ← `createDatasetVersion`（**建版本即自动构建**：成功后立即 `createBuildTask({type:'manual'})` 并 `useJob` 轮询（`buildJobId`/`buildError` state），成功 → `onDatasetChanged` + 刷新版本列表，失败 → 明确报错横幅；manual 来源不指定样本 → 后端兜底按登记焊缝生成成员，修复此前"只能建空版本、构建接口无 UI 消费"的断头路）；成员详情设置 `selectedDataId`，继续复用核验/版本/分析流程；训练/测试数据集条 ← `listDatasets()[0]`）。**Task 23 起分析与标注页已接 API**：`AnalysisSelect` 为**数据集优先两级选择**——第一级 ← `listDatasets` 数据集下拉（失败回退 `mockDatasetRows`、默认取第一个），第二级 ← `listWelds({ dataset_id })` 拉该数据集全部焊缝（失败回退 `mockWeldRows`），**仅核验异常（quality===异常）的卡片 `disabled` 置灰**（待复核可进——上传数据带警告核验后为待复核，若按"非通过即拦"会永远进不了分析流），其余 `onContinue` 进入对齐；`GET /analysis/candidates` 已不再被本页消费（保留兼容）；`Alignment`（含 splitOnly 变体，**2026-08-28 重构为两个视觉差异形态**：`splitOnly=false` → **多模态对齐·时间轴对齐工作室**（`studio-ruler` 共享时间标尺 + 各模态 `lane` 轨道 + 模态勾选 `modalities` state → `createAlignmentTask`；**不再展示切分规则/输出格式/预览按钮**——修复此前对齐页误显示切分控件、且点"创建切分任务"实跑对齐任务的错位）；`splitOnly=true` → **数据切分·样本生产流水线**（`split-steps` 步骤条 + `cut-band` 切分条带 + `cut-summary` 统计 + 规则面板 + `sample-preview-grid` 预览网格））← `createAlignmentTask/createSplitTask` + `useJob`（轮询对齐/切分 Job，成功渲染 events/sample_count；**切分页缓冲秒数可编辑，`keep_event_buffer` 按准确数值透传，关闭时传 0**；**对齐页已真实化（合并 main）**：`listVersions` 找 v1.0 → 按视频扩展名取 key → `getFileUrl` 渲染 `<video>`（`onTimeUpdate` 驱动 playhead），`getSignals(v1.0)` 真实波形画进轨道，`rulerTicks`/`cutTicks` 时间刻度 + availability 角标（title=reason），成功/失败横幅（`event_source` / `job.error.message`），`handleRun` modalities 用 `modalities` state（默认取自 `record.modalities`），成功后 `setVersionId(alignRes.version.id)`）；`AdvancedWeldAnalysis` ← `getSignals`（挂载/滤波变化时拉取，四通道常驻、勾选仅本地过滤）+ `getAnalysisMode`（psd/stft/dwt/wavelet/phase/pdd，mode/目标通道/滤波联动）+ `getAnalysisResult`（稳定度/三类占比/异常区段 chips）；`Annotation` ← `listLabelCategories` + 挂载时 `createAnnotationTask({source:'manual',name:'AN-演示'})`（best-effort）+ `useJob` → `listAnnotationSamples/getAnnotationSample`，AI 预标注 ← `aiPretag`、保存 ← `saveAnnotation`、样本图 ← `getFileUrl`；**标注 kind 升级（时序标注模式）**：`Annotation` 增加 `dataId` prop + 图像/时序/视频三模式切换栏；时序模式 `AnnotationSignal` ← `getWeld` 取最新版本 → `createAnnotationTask({source:'signal', version_id})`（后端创建信号锚点样本）→ `useJob` → `listAnnotationSamples/getAnnotationSample` 取锚点样本与既有 segment 标注 → `getSignals` 拉四通道波形 → ECharts 点击设起点/终点选缺陷区间（`markArea` 高亮，异常段作 AI 提示色带）→ 选类别生成区间 → `saveAnnotation`（labels 带 `kind:'segment'`/`start_time`/`end_time`）；焊缝图片参考 ← `getVersion` 版本 object_keys 挑一张图像 `getFileUrl`；**视频标注模式 `AnnotationVideo`**：`createAnnotationTask({source:'video', version_id})`（后端建视频锚点样本）→ 取锚点 `video_key` → `getFileUrl` 播放 → 播放/暂停捕获帧（`canvas` 按视频自然分辨率取 `dataURL`）→ `react-image-annotate`（`--legacy-peer-deps` 装的，peer 仅 React16）画多边形（归一化坐标，`key` 重挂载重置每帧）→ 点标注器自带保存 `onExit` → 归一化 × 帧宽高转像素 → `createAnnotationFrame` 建帧样本 → `saveAnnotation`（kind='polygon'，category='熔池'）；已标注帧侧栏展示；`FeatureExtraction` ← `extractFeatures`（"执行提取"工具栏按钮，归一化/格式读 UI 状态，L2 范数↔L2 映射），三类特征表 + 统一向量条由结果映射（保留 mock 初始/兜底）。**Task 24 起模型中心已接 API**：模型中心新增 `model-center/dataset-build` 子菜单「数据集构建」（置于模型仓库之前）→ `DatasetBuild` 页 ← `listDatasets` 选数据集 + 来源单选（manual/split_task/annotation_task）→ `createDatasetVersion` 即自动 `createBuildTask` + `useJob` → 展示 8:1:1 train/val/test（`job.result.split`）+ 样本总数/质量/快照；`ModelRepository` ← `listModels`（汇总总数/生产候选/最近训练/GPU + 模型卡片，`metric` dict→`modelMetricText` 文案），「新建模型」← `createModel` + 仓库刷新计数（`refreshKey`）；`Training` ← `listDatasets()[0]` 当前版本 + `listModels` 解析基础模型（best-effort），开始训练 ← `createTrainingTask`（超参读表单 `config` state）+ `useJob` → 指标/损失曲线（`lossToPath` 数据驱动 SVG path）/进度，日志 ← `getTrainingLogs`；`ModelTest` ← `createTestTask` + `useJob` → 指标 + 2×2 混淆矩阵；`InferencePanel` ← 上传 `uploadFile`(<100MB)/`presignUpload`(≥100MB)+PUT（**PUT 后先查 `res.ok`，失败抛错丢弃 object_key**）→ object_key → `createInferenceTask` + `useJob` → 类别/置信度/耗时；`Toolbar` 导出 ← `exportReport`（type 按页映射 data-list/validation/analysis/annotation/features/test），返回 url → `window.open`。接线模式：总览通过 `getDashboardData` 读取四个真实聚合接口，使用浏览器缓存（5 分钟 TTL）避免每次刷新重复请求；缓存失效后重新请求并覆盖缓存，接口失败显示明确错误状态，不使用 mock 兜底。其他页面按各自接口接线规则处理；JSX/className 尽量不动。
- `index.css`：Tailwind 全局样式。
- `vite-env.d.ts`：Vite 类型声明。
- `api/`：前端接口层（Task 18 起）。`client.ts`（统一 fetch 封装：解包信封、注入 JWT、401 清 token 重载）、`types.ts`（全部实体/请求体类型，契约见 `docs/API接口清单.md`）。详见 `api/CLAUDE.md`。
- `hooks/`：前端 React 钩子层（Task 20 起）。`useJob.ts` 通用异步任务轮询（消费 `api/jobs.getJob`）。详见 `hooks/CLAUDE.md`。
- `pages/`：前端页面层（Task 20 起）。`Login.tsx` 最小登录页（登录成功写 token+user 到 localStorage 并通知外层）。详见 `pages/CLAUDE.md`。
- `app/`：应用级路由与导航配置（Task 21 重构）。`navigation.ts` 定义 `Route` 联合类型 + `navStructure` 侧边栏树 + `workspaceHeaders` 页头文案，是 App.tsx 导航的单一来源。详见 `app/CLAUDE.md`。
- `features/`：按业务域拆分的工作区页面（2026-08-29 重构自 App.tsx）。overview/datasets/registration/validation/versions/analysis/annotation/alignment/models/features/data-context 各一目录，由 App.tsx `WorkspaceFrame` 懒加载。详见 `features/CLAUDE.md` 及各子目录。
- `shared/`：跨 feature 复用件。`components/`（Toolbar/PageIntro/StatusPill/InfoRow/TextDialog）+ `lib/`（formatting）。详见 `shared/CLAUDE.md`。
- `components/`：业务组件。`annotation/AnnotoriousImageEditor.tsx` 统一图像/视频帧标注编辑器（Annotorious 适配层，坐标按原始像素）。详见 `components/CLAUDE.md`。
- `App.buffer-regression.test.mjs`：Node 内置测试；静态断言 `Alignment` 切分页暴露 `bufferSeconds` 可编辑输入，且 `createSplitTask` 不再把 `keep_event_buffer` 硬编码为 `0.2`。
- `vite-base-regression.test.mjs`：Node 内置测试；静态断言生产构建使用根路径 `/`，避免部署在 FastAPI 根挂载点时生成错误的 `/ai_welding/assets/` 资源路径。

坑/限制：
- `App.tsx` 已是**路由组合 + 懒加载**（2026-08-29 重构），不再单文件堆组件。新增页面按「feature 目录 + `app/navigation.ts` 注册路由 + `App.tsx` WorkspaceFrame 懒加载映射」扩展；改动页面时不要破坏四个一级模块信息架构与「先选数据」模式。
- 总览/列表接 API 时的映射约定：分布/缺陷 `tone` 用 `donutPalette`（`src/App.tsx`）按序取色（后端不输出颜色）；多模态 token 经 `modalityMeta` 映射中文 label/icon/desc（video/timeseries/audio/infrared 等）；`Project.progress`(0-100) 字符串化为 `"68%"`、status→tone（标注中→blue，可训练/已完成→green，其余→orange）；`listWelds` 的 `source`/`brand` 为前缀匹配、`tab` 传 `全部最新数据/待核验/已归档`（后端未知 tab 视为不过滤），分页 `page/page_size`。
- 数据集层级（Task 2–4 + final fix）：`DatasetWorkspace` 严格按**数据集列表 → 数据集概览 → 当前版本成员 → 成员详情**呈现，列表页不内嵌概览；真实 `listDatasets` 成功后，选中版本必须重绑定为所选数据集的 `current_version_id`（无当前版本则为 `null`），不得保留 mock 版本。`DatasetDetail` 的版本列表初始必须为空（或仅 dataset-specific 安全回退），`listDatasetVersions` 失败后也不得回灌 `mockDatasetVersions`。无当前版本仅显示“创建数据集版本”空状态，不显示 mock 版本入口。`DatasetItemRow` 是固定 `dataset_items` 快照中的样本粒度行，筛选 `q/quality/split` 必须由 `listDatasetVersionItems(datasetId, versionId, params)` 请求服务端；成员页训练/验证/测试总数取 `getDatasetVersion(...).split`，不从当前分页行归纳；`DatasetRecords` 的版本摘要只能来自当前 `dataset/versionId` 的真实详情，失败时必须显示明确不可用状态，不能借用别的版本 mock 摘要。版本切换清空成员与 `selectedDataId`；`DatasetRecords` 成员行/总数初始必须为空 + `loading` 态（显示"成员列表加载中…"，样本数卡显示"…"），**绝不能用 `mockDatasetItemRows` 作初始值**（会在接口响应前闪现 3 条假数据），mock 行仅允许在成员 API 失败时兜底并给出非阻塞提示。成员详情仅接受行中的真实 `weld_id`，以其调用 `getWeld` 并显示登记编号、当前版本和核验状态及数据集/版本/划分上下文，绝不以 `sample_id` 代替焊缝 ID。`data-center/registration` 是新建操作，绝不加入 `routesRequiringData`。
- 数据中心接 API 的映射约定（Task 22）：数据集行 `DatasetRow` 由 `Dataset` 派生——`split` 空/未构建→`'—'`（详情 `split` 同），`progress`→`"96.8%"`，`status`→tone（可训练→green，其余→orange），`source` 后端无此字段统一 `'多模态数据'`；核验规则由 `ValidationRuleResult.status`(passed/warning/failed) 映射图标/文案/状态色（失败→红、警告→橙、通过→绿），汇总状态 `failed>0→异常 / 仅警告→待复核 / 否则→核验通过`；维度由 `DimensionStatus.status`(已具备/必需/缺失)+`required` 驱动；适配检查 `ReadinessCheck` 的 `checks[].passed` 决定每行图标；数据集版本 `DatasetVersion` 无 label/action 字段，快照行标题用 `数据快照 · {item_count} 条样本`，当前版本按 `detail.current_version_id` 判定；血缘 `LineageNode` 按 `type` 映射图标与后缀（records→条 / training_tasks→次 / 其余→个）。
- 分析页接 API 的映射约定（Task 23）：通道 id `cur/vol/gas/wir` 前后端一致，后端不输出颜色→`chanColor` 按 id 映射；`getSignals` 返回 `values` 已由 api 层抽稀 ≤512，`toPath` 按 `values.length` 归一化横轴（勿再按 mock 的 `SAMPLES`）；六种图表组件（`PsdChart/StftHeatmap/DwtChart/WaveletDecomp/PhasePlot/PddChart`）都改为吃后端数组（`freqs/psd`、`magnitude`、`bands+approx`、`bands`、`current+voltage`、`bins+counts+kde`），未取到 API 时用 `values` 走原内部计算兜底（SVG 结构不动）；`DwtChart`/`WaveletDecomp` 取到 API 后标签用后端 `band.name`（D1..D4+A4 / L1..L5），无 API 才用内部计算标签；滤波在 signals/mode 请求带 `filter_type/cutoff/cutoff2` 时由后端计算（`filter_type` 逐字对齐 `filterTypes`），前端不再本地 `applyFilter`（已删除）；标注页 manual 源标注任务无样本（后端只给 split_task 归属样本），样本图改为从所属焊缝版本 object_keys 取真实图像（`getFileUrl`），无图时显示空态不回落静态演示图；AI 预标注触发挂在标注板工具栏 icon-button（`title="AI 预标注"`）。
- **mock 闪烁全局禁令**：任何会被接口成功响应覆盖的 state，初始值一律不得用 mock 数据（会在接口响应到达前闪现假数据），必须初始为空 + `loading` 态并显示明确加载中文案；mock 仅允许在接口 **catch 失败分支**回填兜底。已按此修复的位置：`DatasetWorkspace` 列表、`DatasetDetail` 的 dims/lineage/qualityPct/updated（无 detail 时显示 `…`）、`DatasetRecords` 成员行、`RawSignalPreview` 波形（meta 显示"信号加载中"；**effect 重跑时先 `setChannels([])` 清空旧波形**——`versionId` 由 `record?.latest_version_id ?? versionId` 派生，`getWeld` 返回后会二次触发拉取，不清空则旧版本波形一直渲染到新响应返回造成闪图；channels 为空且 loading 时图表区显示"波形加载中…"占位）、`VersionPanel` 版本链（删除组件内 mockVersions，失败显示"版本信息暂时无法读取"）、`ModelRepository` summary/models、`Registration` 最近上传、`AdvancedWeldAnalysis` 波形（初始用 `emptyChannels` 空波形占位保留通道骨架，防 `channels[0]` undefined 崩溃）、`Annotation`/`AnnotationSignal` 标签类别、标注页图像模式（假标注框/假缩略图/假分页已删，样本号 `frameLabel` 兜底 `—`、`totalSamples` 初始 0、「标注信息」行无 sample 时显示 `—`；标注页已改用真实焊缝图，静态占位图 `labelImage` 已删除）、`Validation` 核验页（mock 报告仅接口失败/无版本可查时经 `fallback()` 兜底并显示提示横幅，加载期评分 `—`、状态"加载中"、规则区"核验规则加载中…"；mock 规则名单提为模块级 `mockValidationRuleNames`）、`SelectionContext`（初始 `row=null`，getWeld 失败才回 mock 行）、`ModelReadiness`（`loading` prop + `DatasetDetail` 的 `readinessLoading`，加载期显示"模型适配检查加载中…"）。刻意保留 mock 初始的例外：`FeatureExtraction` 特征表（仅用户点"执行提取"才被接口覆盖，属"演示初始/兜底"约定）。
- `package.json` 里的 `@supabase/supabase-js` 依赖当前未使用。

### 当前选择上下文与真实标注约束

- `AppShell` 维护全局 `selectedDatasetId` 与 `selectedDataId`；所有侧栏子菜单均可进入，工作区顶部的 `SelectionSwitcher` 负责切换数据集和焊缝。分析/标注业务在未同时选中两者时只显示引导，不执行任务。
- `Annotation` 图像模式必须从当前焊缝版本的真实图片对象创建 manual 样本；`AnnotationSignal` 只允许 `getSignals().source === 'real'` 的波形进入标注；`AnnotationVideo` 只播放当前版本对象存储中的真实视频。
- **波形两级加载（性能修复，2026-08-28）**：`AnnotationSignal` 首屏 `getSignals(..., { max_points: 2048 })`（服务端 min-max 抽稀，附 `times` 秒坐标，防 910k 点全量 ~26MB 响应在公网拖 50s）；ECharts series 有 `times` 时按 `[t,v]` 画点（非均匀抽稀勿按序号均分），x 轴恒为绝对时间 `[0, dur]`；`dataZoom` 停止（300ms 防抖）后按可见窗口 `{max_points: 4096, start, end}` 增量取细节、拉回全程取回概览，stale 响应用 `fetchTokenRef` 递增 token 丢弃；缩放视口百分比存 `zoomPct` state，窗口重渲染时写回 dataZoom `start/end` 防视图复位。**坑：`getSignals` 传了 `max_points` 后 api 层不再本地 decimate（点数已由服务端控制）**。
- **视频预览兜底**：`AnnotationVideo` 的 `<video>` 挂 `onError` → `videoError` state 显示明确原因（`MediaError.code===4` = 编码浏览器不支持，提示 media_prep 转码预览版稍后重试/转 H.264 重传），不再静默黑屏；后端锚点 `video_key` 优先用 media_prep 转码预览 key（`meta.source_video_key` 为原始 key），转码失败回退原始 key。
- 图像和视频帧标注统一使用 `src/components/annotation/AnnotoriousImageEditor.tsx`；时序标注继续使用 ECharts。Annotorious 的几何坐标按原始图片像素保存，不要恢复旧的 `react-image-annotate` DOM 翻译或归一化回调方案。
- 接入后端时：接口走 `src/api/`，相对路径 `/api/v1/...`（契约见 `docs/API接口清单.md`）；开发环境由 `vite.config.ts` 的 `/api` proxy 转发到 `http://localhost:8000`，生产同源。页面组件放 `src/features/*`，通用件放 `src/shared/*`，勿堆回 `App.tsx`。
